#pragma once

#include <vector>

using namespace std;

class MatrixA {
private:
	int width, height;
	double h, k;

public:
	MatrixA(int _width = 0, int _height = 0, double _h = 0.1, double _k = 0.1);


};
